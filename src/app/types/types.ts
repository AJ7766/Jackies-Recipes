export type ProfileProps = {
  username?: string;
  fullName?: string;
};

export type RegisterFormProps = {
  email: string;
  username: string;
  fullName: string;
  password: string;
  confirmPassword: string;
}
